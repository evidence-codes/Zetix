# apps/android — native Kotlin consumer app (CameraX, TFLite, local Chroma). Phases 2–3 / EPIC-2,3.
# Native tool: Gradle (owns file-level incrementality + caching).
# Replace the placeholder echoes below as EPIC-2/3 lands.
.PHONY: build test lint fmt bootstrap clean

build:     ; @echo "[android] build — not implemented yet (EPIC-2)"         # -> ./gradlew assembleDebug
test:      ; @echo "[android] test — not implemented yet (EPIC-2)"          # -> ./gradlew testDebug
lint:      ; @echo "[android] lint — not implemented yet (EPIC-2)"          # -> ./gradlew ktlintCheck
fmt:       ; @echo "[android] fmt — not implemented yet (EPIC-2)"           # -> ./gradlew ktlintFormat
bootstrap: ; @echo "[android] bootstrap — not implemented yet (EPIC-2)"     # -> verify gradle wrapper / SDK
clean:     ; @echo "[android] clean — nothing to clean yet"                 # -> ./gradlew clean
