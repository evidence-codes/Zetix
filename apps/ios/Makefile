# apps/ios — native Swift consumer app (AVFoundation, Core ML). Phase 4 / EPIC-4.
# Native tool: Xcode + SPM (owns file-level incrementality + caching).
# Replace the placeholder echoes below as EPIC-4 lands.
.PHONY: build test integration-test lint fmt bootstrap clean

build:            ; @echo "[ios] build — not implemented yet (EPIC-4)"               # -> xcodebuild build
test:             ; @echo "[ios] test — not implemented yet (EPIC-4)"                # -> xcodebuild test (unit)
integration-test: ; @echo "[ios] integration-test — not implemented yet (EPIC-4)"   # -> xcodebuild test (integration)
lint:             ; @echo "[ios] lint — not implemented yet (EPIC-4)"                # -> swiftlint
fmt:              ; @echo "[ios] fmt — not implemented yet (EPIC-4)"                 # -> swiftformat .
bootstrap:        ; @echo "[ios] bootstrap — not implemented yet (EPIC-4)"           # -> swift package resolve
clean:            ; @echo "[ios] clean — nothing to clean yet"                       # -> xcodebuild clean
