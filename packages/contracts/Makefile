# packages/contracts — shared API + sync-protocol + vector-format schemas (the version-locked spine).
# Consumed by server, both apps, and both SDKs. EPIC-0.5 / EPIC-1+.
# `gen` produces the per-language client/server types from the source schemas.
.PHONY: gen build test clean

gen:   ; @echo "[contracts] gen — not implemented yet (EPIC-0.5)"   # -> codegen from openapi.yaml + sync-protocol
build: gen
test:  ; @echo "[contracts] test — not implemented yet (EPIC-0.5)"  # -> validate schemas / contract tests
clean: ; @echo "[contracts] clean — nothing to clean yet"
