# packages/sdk-android — distributable Android AAR (CameraSearchView + API). Phase 5 / EPIC-5.
# Native tool: Gradle.
.PHONY: build test lint fmt clean

build: ; @echo "[sdk-android] build — not implemented yet (EPIC-5)"   # -> ./gradlew assembleRelease
test:  ; @echo "[sdk-android] test — not implemented yet (EPIC-5)"    # -> ./gradlew test
lint:  ; @echo "[sdk-android] lint — not implemented yet (EPIC-5)"    # -> ./gradlew ktlintCheck
fmt:   ; @echo "[sdk-android] fmt — not implemented yet (EPIC-5)"     # -> ./gradlew ktlintFormat
clean: ; @echo "[sdk-android] clean — nothing to clean yet"
