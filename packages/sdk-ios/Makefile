# packages/sdk-ios — distributable iOS Swift Package (SwiftUI View + UIViewController). Phase 5 / EPIC-5.
# Native tool: SPM + Xcode.
.PHONY: build test lint fmt clean

build: ; @echo "[sdk-ios] build — not implemented yet (EPIC-5)"   # -> swift build -c release
test:  ; @echo "[sdk-ios] test — not implemented yet (EPIC-5)"    # -> swift test
lint:  ; @echo "[sdk-ios] lint — not implemented yet (EPIC-5)"    # -> swiftlint
fmt:   ; @echo "[sdk-ios] fmt — not implemented yet (EPIC-5)"     # -> swiftformat .
clean: ; @echo "[sdk-ios] clean — nothing to clean yet"
