# plugins/shopify — Shopify App Store plugin (hosted storefront injection, no-code setup). Phase 5 / EPIC-5.
# Native tool: TBD (Shopify CLI / app stack), decided in EPIC-5.
.PHONY: build test lint fmt clean

build: ; @echo "[shopify] build — not implemented yet (EPIC-5)"
test:  ; @echo "[shopify] test — not implemented yet (EPIC-5)"
lint:  ; @echo "[shopify] lint — not implemented yet (EPIC-5)"
fmt:   ; @echo "[shopify] fmt — not implemented yet (EPIC-5)"
clean: ; @echo "[shopify] clean — nothing to clean yet"
